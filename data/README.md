# Data
`LeukSurv.csv` and `nwengland.bnd` are exported from the `spBayesSurv` R package
(version X.Y.Z), via `data(LeukSurv)` and `data(nwengland)`. The leukemia survival
data originate with Henderson, Shimakura and Gorst (2002), JASA 97, 965-972.
